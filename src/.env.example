PORT=4000
DATABASE_URL="file:./dev.db"
JWT_SECRET=change_this
WEB3_RPC_URL=http://127.0.0.1:8545    # or Mumbai RPC
PRIVATE_KEY=0x...                     # signer for backend registry writes (never commit)
INVOICE_REGISTRY_ADDRESS=             # set after deploy
USE_SIMULATED_CHAIN=true              # toggles on-chain vs simulated